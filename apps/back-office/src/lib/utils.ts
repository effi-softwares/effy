// shadcn generates `import { cn } from "@/lib/utils"`. The cn util lives ONCE in the shared
// design-system (the brand SSOT); this re-export keeps shadcn's generated imports working.
export { cn } from "@effy/design-system";
