{
  "name": "@effy/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Effy web API client — one authed fetch wrapper + RFC 9457 error mapping. Consumes @effy/shared-types. Internal package: exports TypeScript source.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests\" && exit 0"
  },
  "dependencies": {
    "@effy/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
