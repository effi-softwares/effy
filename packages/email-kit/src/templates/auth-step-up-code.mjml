<!-- auth-step-up-code — Cognito's native MFA / step-up verification code.
     ⚠ SENT BY COGNITO. Code is `{####}`; no `code` variable. See auth-sign-up-code.mjml for the rules.

     ⚠ THIS IS NOT THE PASSWORDLESS SIGN-IN CODE. That code is issued by 035's custom challenge and
     sent by the platform itself as `auth-sign-in-code`. This template is only for Cognito's own
     `CustomMessage_Authentication` (native MFA), which is a different mechanism. Conflating the two
     would put two different messages on one template. -->

<mjml lang="en" dir="ltr">
  <mj-head>
    <mj-include path="../generated/theme.mjml" />
    <mj-title>Your {{effyProductName}} verification code</mj-title>
    <mj-preview>Enter this code to continue.</mj-preview>
  </mj-head>

  <mj-body width="600px" background-color="#f5f5f5" css-class="e-bg-page">
    <mj-include path="../components/header.mjml" />

    <mj-section padding="0 32px 32px 32px">
      <mj-column>
        <mj-text mj-class="h1" css-class="e-ink e-h1">Your verification code</mj-text>

        <mj-text mj-class="muted" css-class="e-muted" padding="12px 0 24px 0">
          Enter this code to continue signing in to {{effyProductName}}. It can only be used once.
        </mj-text>

        <mj-text mj-class="code" css-class="e-ink e-bg-code" align="center"
                 container-background-color="#f5f5f5" padding="24px 16px">{####}</mj-text>

        <mj-text mj-class="small" css-class="e-muted" padding="24px 0 0 0">
          If you didn&rsquo;t try to sign in, you can ignore this email &mdash; nobody can use the code
          without it.
        </mj-text>
      </mj-column>
    </mj-section>

    <mj-include path="../components/footer.mjml" />
  </mj-body>
</mjml>
