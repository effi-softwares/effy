{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "ChangePasswordDTO": {
      "additionalProperties": false,
      "description": "Changing an EXISTING password. Cognito verifies `currentPassword` itself (FR-016).",
      "properties": {
        "currentPassword": {
          "type": "string"
        },
        "mode": {
          "const": "change",
          "type": "string"
        },
        "newPassword": {
          "type": "string"
        }
      },
      "required": [
        "mode",
        "currentPassword",
        "newPassword"
      ],
      "type": "object"
    },
    "ClosureBlockerDTO": {
      "additionalProperties": false,
      "properties": {
        "clearsAt": {
          "description": "⚠ NEVER null. FR-042 forbids a block that cannot state its own end.",
          "type": "string"
        },
        "href": {
          "description": "Where the customer goes to act on it — FR-042's \"direct route\". Web routes on this.",
          "type": "string"
        },
        "kind": {
          "$ref": "#/definitions/ClosureBlockerKind"
        },
        "reference": {
          "description": "Shopper-facing reference, e.g. `EFY-HVX2AE`.",
          "type": "string"
        },
        "resolvableByShopper": {
          "description": "Can the customer act on it, or only wait? Both are acceptable; a dead end is not.",
          "type": "boolean"
        },
        "target": {
          "additionalProperties": false,
          "description": "The same destination in the closed vocabulary mobile needs, having no URL router.",
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "const": "order",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "id"
          ],
          "type": "object"
        }
      },
      "required": [
        "kind",
        "reference",
        "href",
        "target",
        "clearsAt",
        "resolvableByShopper"
      ],
      "type": "object"
    },
    "ClosureBlockerKind": {
      "description": "Why a customer cannot be deleted yet (034 FR-042).\n\n⚠ THIS REQUIREMENT HAS BEEN WRONG TWICE, so the shape encodes the lesson rather than trusting a comment. First it blocked on any non-terminal order — but an order's only terminal state is a fulfilment reaching `collected`, which ships behind a dev-only stub with no route in any environment, so EVERY customer who had ever paid became permanently undeletable. The fix bounded it at 30 days, matched to the grace period — and that was the same dead end in disguise, because Effy is a WEEKLY-RE-BUY grocery platform: a shopper who buys every week is always within 30 days of an order, so the platform's most engaged customers still could never delete.\n\nHence `clearsAt` is NON-NULLABLE: a blocker that cannot say when it ends is unrepresentable, not merely discouraged.",
      "enum": [
        "order_awaiting_payment",
        "order_in_transit"
      ],
      "type": "string"
    },
    "ClosureChallengeResultDTO": {
      "additionalProperties": false,
      "description": "The step-up challenge for closure — the same masked-destination shape the password flow uses.",
      "properties": {
        "maskedDestination": {
          "type": "string"
        }
      },
      "required": [
        "maskedDestination"
      ],
      "type": "object"
    },
    "ClosurePreviewDTO": {
      "additionalProperties": false,
      "description": "Everything the customer must see BEFORE any irreversible step (FR-040). Side-effect free.",
      "properties": {
        "activeRequest": {
          "anyOf": [
            {
              "additionalProperties": false,
              "properties": {
                "eraseAfter": {
                  "type": "string"
                },
                "requestedAt": {
                  "type": "string"
                }
              },
              "required": [
                "requestedAt",
                "eraseAfter"
              ],
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "description": "Present only when a request is already live."
        },
        "blockers": {
          "description": "Empty ⇒ closure may proceed.",
          "items": {
            "$ref": "#/definitions/ClosureBlockerDTO"
          },
          "type": "array"
        },
        "eraseAfterIfRequestedNow": {
          "description": "Advisory until a request actually exists.",
          "type": "string"
        },
        "retained": {
          "items": {
            "$ref": "#/definitions/RetainedCategoryDTO"
          },
          "type": "array"
        }
      },
      "required": [
        "blockers",
        "retained",
        "eraseAfterIfRequestedNow",
        "activeRequest"
      ],
      "type": "object"
    },
    "ClosureRequestDTO": {
      "additionalProperties": false,
      "description": "Confirming closure. The code is what pays for it (FR-043); a valid session alone may not.",
      "properties": {
        "code": {
          "type": "string"
        }
      },
      "required": [
        "code"
      ],
      "type": "object"
    },
    "ClosureRestoreResultDTO": {
      "additionalProperties": false,
      "description": "Cancelling a live closure request during the grace window (FR-041a).\n\n⚠ RESTORE IS AN EXPLICIT CALL, never an inference from an authenticated read. Making it implicit is unimplementable — the refusal and the restore run through the SAME identity lookup, so the gate would refuse the very request meant to restore — and unsafe, because anyone holding the customer's token during the window would silently un-delete the account merely by opening the app.",
      "properties": {
        "restoredAt": {
          "type": "string"
        }
      },
      "required": [
        "restoredAt"
      ],
      "type": "object"
    },
    "ClosureResultDTO": {
      "additionalProperties": false,
      "properties": {
        "allSessionsRevoked": {
          "const": true,
          "type": "boolean"
        },
        "eraseAfter": {
          "description": "The date the customer is now owed (FR-040).",
          "type": "string"
        }
      },
      "required": [
        "eraseAfter",
        "allSessionsRevoked"
      ],
      "type": "object"
    },
    "ClosureState": {
      "description": "Where a customer sits in the closure lifecycle (034).\n\n`\"closing\"` means they asked to be deleted and the grace window is running. Every request is refused while it holds — with exactly ONE exception, the explicit restore call (FR-041a).",
      "enum": [
        "open",
        "closing"
      ],
      "type": "string"
    },
    "CredentialRoute": {
      "description": "How a customer proved who they are. Telemetry + UI only — never an authorization input.",
      "enum": [
        "password",
        "otp",
        "google"
      ],
      "type": "string"
    },
    "CustomerDTO": {
      "additionalProperties": false,
      "description": "The platform's own record of a customer — distinct from their Cognito credential, and authoritative for the access decision (FR-025).\n\nA `barred` customer is refused no matter how valid their credential is. That is the whole point of holding our own record: the claim is the ORIGIN of identity, the record is the AUTHORITY on access.\n\nNote there is no `role` here, and none is coming: the customer pool defines no RBAC groups (constitution Principle IV). That is also a token-size safety measure — id + access + refresh cookies already run to ~4.5 KB against a ~4 KB browser limit, and a fattened claim set would silently truncate the session.",
      "properties": {
        "closureState": {
          "$ref": "#/definitions/ClosureState",
          "description": "Has this customer asked to be deleted? (034 FR-041.)\n\n⚠ DELIBERATELY NOT a third value of `status`. `status` is a PLATFORM SANCTION, and its entire safety property is that the customer cannot influence it; closure is the customer's OWN decision. Collapsing them would make \"barred AND closing\" unrepresentable — and FR-049 requires an answer to exactly that case.\n\nThere is no `\"closed\"`: once erasure runs, the row is gone. A terminal state would be a row every future read must remember to exclude."
        },
        "createdAt": {
          "type": "string"
        },
        "email": {
          "description": "The verified email. It is the identity key across every credential route.",
          "type": "string"
        },
        "familyName": {
          "type": [
            "string",
            "null"
          ]
        },
        "givenName": {
          "description": "First and last name, captured AT REGISTRATION (FR-009a) and mapped 1:1 onto Cognito's standard `given_name` / `family_name` attributes — so they ride on the ID token with no custom claim.\n\nTwo fields, not one: a delivery label, an order confirmation and a support conversation all need the parts, and a single free-text name cannot be split back into them reliably (ask anyone with two surnames, or one name). Captured at source; never inferred.\n\nNullable because the FEDERATED route supplies whatever the provider asserts, and may assert neither. The platform must not invent a name it was never given.",
          "type": [
            "string",
            "null"
          ]
        },
        "hasPassword": {
          "description": "Does this account have a password? (012 FR-013.)\n\n⚠ THE PLATFORM MUST HOLD THIS ITSELF, because Cognito cannot be asked. There is no API that reports whether a user has a password: `AdminGetUser` does not return it, and `UserStatus` does not distinguish it — a passwordless CONFIRMED user and an email+password CONFIRMED user are identical on the wire (research R5).\n\n⚠ AND IT IS THE ONLY THING THE ACCOUNT PAGE MAY BRANCH ON. Never branch on \"how did they sign in\": a Google-LINKED customer is an ordinary native user and CAN hold a password. Inferring from the sign-in route would show the wrong control to a real cohort.",
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "passwordUpdatedAt": {
          "description": "When the password last changed. `null` means NEVER — which is a legitimate, complete, permanent state for an email-OTP customer, not a missing value and not an incomplete profile (FR-015).",
          "type": [
            "string",
            "null"
          ]
        },
        "phone": {
          "description": "A self-asserted contact phone (034 FR-060).\n\n⚠ NEVER VERIFIED by feature 034, and that has two hard consequences (FR-060a): it MUST NOT be rendered with a verified/confirmed indicator, and it MUST NOT be accepted by any identity, recovery or authentication path. There is deliberately no `phoneVerified` companion — a field whose only honest value is `false` is a trap, because someone will eventually render it as a badge.\n\n⚠ DISTINCT from `AddressDTO.phone`, which is the per-address DELIVERY contact a driver calls (FR-060b). This one is a profile convenience; it does not override the address's, and nothing copies one into the other. Two fields disagreeing about who to ring is worse than one gap.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/definitions/CustomerStatus"
        }
      },
      "required": [
        "id",
        "email",
        "givenName",
        "familyName",
        "phone",
        "status",
        "closureState",
        "hasPassword",
        "passwordUpdatedAt",
        "createdAt"
      ],
      "type": "object"
    },
    "CustomerStatus": {
      "description": "A customer's standing with Effy. PLATFORM-OWNED — never derived from a token claim.",
      "enum": [
        "active",
        "barred"
      ],
      "type": "string"
    },
    "PasswordChallengeResultDTO": {
      "additionalProperties": false,
      "description": "The step-up challenge result. It carries a MASKED destination and nothing else — never the full address (an information leak) and obviously never the code.",
      "properties": {
        "maskedDestination": {
          "description": "e.g. `j•••@example.com`",
          "type": "string"
        }
      },
      "required": [
        "maskedDestination"
      ],
      "type": "object"
    },
    "PasswordWriteDTO": {
      "anyOf": [
        {
          "$ref": "#/definitions/SetPasswordDTO"
        },
        {
          "$ref": "#/definitions/ChangePasswordDTO"
        }
      ]
    },
    "PasswordWriteResultDTO": {
      "additionalProperties": false,
      "description": "What the storefront must do after a successful password write.\n\n`allSessionsRevoked` is always true (FR-024): Cognito's revocation is all-or-nothing, so a password change ends EVERY session, including the one that made it. The customer is returned to sign-in to prove the new password. See research R7 — and note the window is not zero.",
      "properties": {
        "allSessionsRevoked": {
          "const": true,
          "type": "boolean"
        },
        "customer": {
          "$ref": "#/definitions/CustomerDTO"
        }
      },
      "required": [
        "customer",
        "allSessionsRevoked"
      ],
      "type": "object"
    },
    "ProblemFieldIssue": {
      "additionalProperties": false,
      "description": "RFC 9457 problem+json — the platform's single machine-readable error shape (mirrors docs/api/error-envelope.md from 004). Typed ONCE here (Principle II); every web surface consumes it, never re-declares it.",
      "properties": {
        "field": {
          "description": "The offending field path — or, for a whole-request refusal, a STABLE MACHINE-READABLE CODE.\n\n⚠ 032 uses the second form for delivery-pricing refusals (`cap_below_floor`, `bands_required`, …). \"Please check the fields and try again\" tells an operator nothing about which of five rules they broke, and every one of those rules fails SILENTLY in production if it is not understood — a cap below the floor makes every delivery cost the cap, forever.",
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "field",
        "message"
      ],
      "type": "object"
    },
    "ProblemJSON": {
      "additionalProperties": {},
      "properties": {
        "detail": {
          "type": "string"
        },
        "fields": {
          "items": {
            "$ref": "#/definitions/ProblemFieldIssue"
          },
          "type": "array"
        },
        "instance": {
          "type": "string"
        },
        "status": {
          "type": "number"
        },
        "title": {
          "type": "string"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "title",
        "status"
      ],
      "type": "object"
    },
    "ResetConfirmDTO": {
      "additionalProperties": false,
      "description": "Recovery confirm — the \"forgot password\" completion, moved behind the backend (FR-022b).",
      "properties": {
        "code": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "newPassword": {
          "type": "string"
        }
      },
      "required": [
        "email",
        "code",
        "newPassword"
      ],
      "type": "object"
    },
    "RetainedCategoryDTO": {
      "additionalProperties": false,
      "description": "A category of data kept after erasure, and why (FR-045).\n\n⚠ The reason is carried as data, not hardcoded per surface, because SC-010 requires every claim in the disclosure to be TRUE of the built system — and Apple has demanded that developers cite the specific law behind a retention claim. One source, two surfaces, no drift.",
      "properties": {
        "category": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "category",
        "reason"
      ],
      "type": "object"
    },
    "SetPasswordDTO": {
      "additionalProperties": false,
      "description": "Setting a FIRST password. The `code` is what pays for it (FR-017) — a session alone may not.",
      "properties": {
        "code": {
          "description": "The step-up code, emailed to the account's verified address at the time of the request.",
          "type": "string"
        },
        "mode": {
          "const": "set",
          "type": "string"
        },
        "newPassword": {
          "type": "string"
        }
      },
      "required": [
        "mode",
        "code",
        "newPassword"
      ],
      "type": "object"
    },
    "UpdateCustomerDTO": {
      "additionalProperties": false,
      "description": "What a customer may change about themselves (FR-026).\n\n`email` is deliberately absent: changing it is an identity operation, and a customer who can rewrite their own email can walk onto another customer's record (the well-known Cognito takeover). `status` is absent because it is platform-owned. `hasPassword` is absent because it is a CONSEQUENCE of the password endpoints, never an input to them.\n\n⚠ `closureState` IS ABSENT FOR THE SAME REASON AS `status`, and the omission is load-bearing. Closure is written only by the closure endpoints, after a freshly issued verification code. A profile PATCH must never be able to open or close an account — it would turn a routine name edit into an un-delete, and it would bypass the proof-of-control FR-043 exists to demand.",
      "properties": {
        "familyName": {
          "type": [
            "string",
            "null"
          ]
        },
        "givenName": {
          "type": [
            "string",
            "null"
          ]
        },
        "phone": {
          "description": "⚠ Send `\"\"` to CLEAR it, never `null`.\n\nThe mobile client serialises with `explicitNulls = false`, which drops nulls from the payload entirely — so a `null` phone is indistinguishable from \"field not sent\" and the clear silently no-ops. The backend maps `\"\"` → `NULL`, on the identical path the name parts already use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "givenName",
        "familyName",
        "phone"
      ],
      "type": "object"
    }
  },
  "description": "Aggregator — codegen entry only (see file header). Every field forces a type into the schema.",
  "properties": {
    "changePassword": {
      "$ref": "#/definitions/ChangePasswordDTO"
    },
    "closureBlocker": {
      "$ref": "#/definitions/ClosureBlockerDTO"
    },
    "closureBlockerKind": {
      "$ref": "#/definitions/ClosureBlockerKind"
    },
    "closureChallengeResult": {
      "$ref": "#/definitions/ClosureChallengeResultDTO"
    },
    "closurePreview": {
      "$ref": "#/definitions/ClosurePreviewDTO"
    },
    "closureRequest": {
      "$ref": "#/definitions/ClosureRequestDTO"
    },
    "closureRestoreResult": {
      "$ref": "#/definitions/ClosureRestoreResultDTO"
    },
    "closureResult": {
      "$ref": "#/definitions/ClosureResultDTO"
    },
    "closureState": {
      "$ref": "#/definitions/ClosureState"
    },
    "credentialRoute": {
      "$ref": "#/definitions/CredentialRoute"
    },
    "customer": {
      "$ref": "#/definitions/CustomerDTO"
    },
    "passwordChallengeResult": {
      "$ref": "#/definitions/PasswordChallengeResultDTO"
    },
    "passwordWrite": {
      "$ref": "#/definitions/PasswordWriteDTO"
    },
    "passwordWriteResult": {
      "$ref": "#/definitions/PasswordWriteResultDTO"
    },
    "problem": {
      "$ref": "#/definitions/ProblemJSON"
    },
    "resetConfirm": {
      "$ref": "#/definitions/ResetConfirmDTO"
    },
    "retainedCategory": {
      "$ref": "#/definitions/RetainedCategoryDTO"
    },
    "setPassword": {
      "$ref": "#/definitions/SetPasswordDTO"
    },
    "status": {
      "$ref": "#/definitions/CustomerStatus"
    },
    "updateCustomer": {
      "$ref": "#/definitions/UpdateCustomerDTO"
    }
  },
  "required": [
    "status",
    "closureState",
    "customer",
    "updateCustomer",
    "credentialRoute",
    "setPassword",
    "changePassword",
    "passwordWrite",
    "resetConfirm",
    "passwordChallengeResult",
    "passwordWriteResult",
    "closureBlockerKind",
    "closureBlocker",
    "retainedCategory",
    "closurePreview",
    "closureChallengeResult",
    "closureRequest",
    "closureResult",
    "closureRestoreResult",
    "problem"
  ],
  "type": "object",
  "title": "CustomerContract"
}
