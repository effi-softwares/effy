# air live-reload config for the docker-compose dev loop (research B8).
root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/core-api ./cmd/core-api"
bin = "./tmp/core-api"
include_ext = ["go"]
exclude_dir = ["tmp"]
delay = 300

[log]
time = true
